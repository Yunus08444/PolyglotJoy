name: polyglot
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # Network
  dio: ^5.2.5
  
  # Storage
  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.2.2  # ← Добавлено
  
  # State Management
  provider: ^6.0.5
  
  # UI
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true